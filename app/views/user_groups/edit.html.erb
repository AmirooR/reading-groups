
<%= form_for(@user_group) do |f| %>
 <% if @user_group.errors.any? %>
  <div id="error_explanation">
   <h2><%= pluralize(@user_group.errors.count, "error") %> prohibited this comment from being saved:</h2>
      <ul>
      <% @user_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>

  </div>
 <% end %>
 <div class="field">
	<%= f.label :num_read, "Read number of pages" %>
	<%= f.number_field :num_read %>
 </div>
 <div class="actions">
  <%= f.submit %>
 </div>
<% end %>
